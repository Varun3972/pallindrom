#include <stdio.h>

int main() 
{ int n, sum=0,r,temp; 
printf("Enter a number");
scanf("%d",&n); 
while (n>0) 
{

temp=n;

r=n%10;

sum = sum * 10 + r ;

n=n/10;

}
if(sum==temp)

   printf("%d is a palindrome number", temp);

else 
   printf("%d is not a palindrome number",temp);
 Return 0;
}
